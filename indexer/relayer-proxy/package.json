{
  "name": "smart-account-relayer-proxy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production"
  },
  "dependencies": {
    "@openzeppelin/relayer-plugin-channels": "^0.14.0",
    "hono": "^4.12.3"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.69.0"
  }
}
